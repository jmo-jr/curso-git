# Curso de Git

## Primeiros passos

git config --version

git config --list

git config --global user.name "Nome"

git config --global user.email "mail@mail.com"

git init

git status

git add ARQUIVO.md

git commit -m "Descrição"

git log